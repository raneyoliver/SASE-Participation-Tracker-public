<!DOCTYPE html>
<html>
  <head>
    <title>SASE Participation Tracker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      html, body, #root {
        height: 100%;
      }
      body {
        margin: 0px;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
